interface:
  display_name: "维护 Agent 协作协议"
  short_description: "生成、审查和维护 AGENTS/CLAUDE 协作协议"
  default_prompt: "使用 $maintain-agent-protocols 生成或维护我的 AI Agent 协作协议，按模型选择 AGENTS.md/CLAUDE.md，判断原则、约束、工作流和检查项应维护在哪里，并优化编号、触发入口与规则路由。"
