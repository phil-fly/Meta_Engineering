# 路由卡片模板

```markdown
## <Route Name>

## 适用场景

- 说明何时读取本路由。
- 说明本路由是已验证项目路由、条件适用路由，还是完整版通用路由。

## 必读入口

- 相关路由：
- 相关场景手册：
- 相关检查清单：

## 核心规则

- 使用可执行、可验证的规则表达。
- 未经目标仓证据验证的规则使用 `应`、`建议` 或 `如项目采用...`，不要写成项目级 `必须`。
- 避免项目私有路径和一次性 workaround。

## 验证与输出

- 说明完成时应验证什么。
- 说明最终回复应包含什么。
```
