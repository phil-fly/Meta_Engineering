# Gateway

适用 API 网关、路由、限流、认证前置和边界保护。

核心规则：

- 网关策略必须显式、可审查、可回滚。
- 新路由暴露前必须明确后端目标、路径匹配规则、认证策略、超时和限流意图。
- 网关安全控制必须失败关闭，不能解析策略时不得默认放行。
- 路由和策略下发必须避免隐藏全局影响。
- 网关不得成为绕过服务端权限的替代入口。
- 如项目采用 Spec-first 或 central change 流程，新增网关路由应能关联到对应规范或 change 锚点。

验证与输出：

- 说明路由、认证、限流、失败关闭和审计边界。
