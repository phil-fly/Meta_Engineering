# 依赖与安全配置

依赖和配置必须遵循最小暴露、可追踪和失败关闭原则。

## 核心规则

- 依赖必须跟踪版本和漏洞风险，高危漏洞应及时升级或记录例外。
- 禁止使用默认凭证、调试端点、详细生产错误和不必要功能。
- 安全配置无法解析或缺失时必须失败关闭，不得默认放行。
- 安全头、CORS、网关策略、认证前置和环境差异必须显式可审查。
- 配置样例不得包含真实密钥、连接串、令牌或私有地址。

## 输出要求

- 说明依赖风险、配置默认值、失败行为和例外依据。
