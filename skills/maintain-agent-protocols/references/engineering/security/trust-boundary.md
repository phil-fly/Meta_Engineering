# 信任边界输入校验

所有来自用户、外部系统、队列、文件、环境变量和跨服务调用的输入，都必须在信任边界校验。

## 核心规则

- 校验必填字段、类型、长度、格式、枚举值和业务允许范围。
- 不要把前端校验、客户端 SDK 或调用方约定当作服务端可信依据。
- 校验失败必须返回稳定错误语义，不能继续进入业务、查询、解释器或资源定位逻辑。
- 文件、环境变量、队列消息、Webhook 和跨服务响应也属于不受信输入。

## 输出要求

- 说明输入来源、信任边界和校验位置。
- 说明未覆盖输入和剩余风险。
