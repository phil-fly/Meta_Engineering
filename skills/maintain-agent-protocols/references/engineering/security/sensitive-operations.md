# 敏感操作控制

删除、导出、上传、权限变更、配置变更、异步任务触发等敏感操作，必须声明并执行适用安全控制。

## 核心规则

- 敏感操作实现前必须盘点认证、授权、资源归属、输入校验、审计和防滥用控制。
- 删除、批量、导出和管理操作必须与同资源 CRUD 保持一致权限边界。
- 高影响操作不得只依赖隐藏前端入口或框架默认行为。
- 重复触发、批量触发、公开入口和昂贵操作必须考虑速率限制或等价防滥用机制。
- 操作完成后必须产生足够审计上下文。

## 输出要求

- 列出敏感操作清单和每项控制状态。
- 对缺失控制给出阻断、补齐或后续任务分类。
