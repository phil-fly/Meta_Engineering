# 安全敏感整数

涉及资源标识、租户边界、权限范围、批量条件、额度、计数等安全敏感语义的整数输入，必须先校验再转换。

## 核心规则

- 数值化前先校验字面格式，拒绝空值、混合字符、科学计数法或不稳定格式。
- 校验值是否处于运行时安全整数范围内，拒绝溢出、精度丢失或截断。
- 与权限、资源归属或批量条件相关的整数，不得在失真后继续参与判断。
- 前端、后端和数据库边界都要保持一致整数语义。

## 输出要求

- 说明整数来源、格式规则、范围规则和拒绝行为。
