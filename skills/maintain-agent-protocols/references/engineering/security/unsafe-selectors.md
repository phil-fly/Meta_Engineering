# 危险选择器安全中介

当不受信输入影响排序字段、文件路径、目标 URL、模板名、脚本表达式、查询条件或资源定位时，必须经过安全中介。

## 核心规则

- 排序字段必须使用白名单映射，禁止直接透传底层字段名或表达式。
- 文件路径必须规范化并限制在允许根目录内。
- 目标 URL 必须校验协议、主机、端口、内网地址和重定向行为。
- 模板名、脚本表达式和动态解释器输入必须使用固定枚举或安全映射。
- 原始不受信选择器不得直接进入查询解释器、文件系统、远程请求或脚本执行器。

## 输出要求

- 说明选择器来源、允许集合和映射策略。
- 说明拒绝策略和失败默认值。
