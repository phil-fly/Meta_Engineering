# Go 错误与资源

## 核心规则

- `context.Context` 必须从入口向下传递，不得保存为结构体长期字段。
- 调用下游、数据库、缓存和外部服务时必须尊重 context 取消和超时。
- 错误应保留上下文并支持判定；用户可见错误和开发者日志上下文必须分离。
- 注释规范继承 `common-quality.md`；本文件只补充 Go 错误、日志和资源生命周期规则。
- 面向终端用户的错误消息必须简洁、明确、可操作；详细排障信息进入日志。

## 输出要求

- 说明错误表达、日志上下文和资源释放。
