# Java 安全与校验

## 核心规则

- 依赖必须可追踪版本，已知高危漏洞必须及时处理或说明例外。
- 密钥、连接串、令牌不得写入代码、配置样例明文或日志。
- 批量查询优先替代循环内查询，避免 N+1。
- 受保护接口必须先经过统一认证门，再进入业务权限判断。
- 权限矩阵必须使用稳定角色与模块标识，不得在局部路径里引入一次性角色别名。
- 说明 validation、transaction、exception mapping 和资源释放是否明确。
- 说明线程池、阻塞操作、反序列化和依赖安全是否覆盖。

## 输出要求

- 说明输入校验、敏感信息、依赖和安全整数。
