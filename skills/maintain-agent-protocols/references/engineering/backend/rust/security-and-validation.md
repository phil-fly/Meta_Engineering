# Rust 安全与校验

## 核心规则

- 性能热点应关注不必要 clone、循环分配、锁粒度、N+1 查询和阻塞 I/O。
- 交付前应考虑格式、lint 和测试入口，安全敏感路径补充针对性用例。
- 受保护接口必须先经过统一认证门，再进入业务权限判断。
- 面向用户的错误描述必须可操作；详细排障信息应保留在日志上下文中。
- 说明 `Result`、输入校验、async 阻塞、unsafe 和并发边界是否清晰。
- 说明资源释放、依赖安全和性能热点是否覆盖。

## 输出要求

- 说明输入校验、敏感信息、依赖和安全整数。
